rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /beta_reports/{reportId} {
      allow read, write: if reportId is string && reportId.size() > 0;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
